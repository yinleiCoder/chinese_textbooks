org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# 关闭 Kotlin 增量编译。
#
# 原因：在 Windows 上编译含 Kotlin 源码的插件（如 shared_preferences_android）时，
# 增量缓存会随机出现「Storage ... is already registered」并导致
# `Could not close incremental caches` 构建失败，清理 build 目录与重启
# Gradle 守护进程都无法稳定复现或修复。
# 这个开关是 Kotlin 官方针对该问题的规避方案；代价是插件模块的 Kotlin
# 编译不再增量，对本项目的构建耗时影响很小（应用自身的 Dart 侧完全不受影响）。
kotlin.incremental=false
